package config;

public class Config {

    // OPTIONS:
    // "TRUE"    → record video for all tests
    // "FALSE" → record video only for failed tests
    public static final String VIDEO_RECORDING = "true";

}
